const myPromise = new Promise(() => {
    
    // 비동기 작업 처리
    setTimeout(()=>{
        const text =prompt("Hello를 입력해줘! 그러면 선물을 줄께!")
        if(text ==='Hello'){
            
        }
    },2000)
})